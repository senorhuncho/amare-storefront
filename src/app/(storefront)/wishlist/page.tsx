import { StorefrontPlaceholderPage } from "@/components/layout/StorefrontPlaceholderPage";

export default function WishlistPage() {
  return (
    <StorefrontPlaceholderPage
      eyebrow="WISHLIST"
      title="Wishlist persistence is still in preview mode."
      description="The homepage cards can simulate saving products locally, but the full wishlist surface will arrive after the storefront commerce flows are connected."
    />
  );
}
