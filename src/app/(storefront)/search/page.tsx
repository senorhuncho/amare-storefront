import { StorefrontPlaceholderPage } from "@/components/layout/StorefrontPlaceholderPage";

export default function SearchPage() {
  return (
    <StorefrontPlaceholderPage
      eyebrow="SEARCH"
      title="Search UI joins the storefront after the browse foundation is in place."
      description="We will connect typed search results and Shopify-backed discovery once the core catalog and collection experience are approved."
    />
  );
}
