import { StorefrontPlaceholderPage } from "@/components/layout/StorefrontPlaceholderPage";

export default function AccountPage() {
  return (
    <StorefrontPlaceholderPage
      eyebrow="ACCOUNT"
      title="Customer account entry is queued for the Shopify connection milestone."
      description="This temporary page keeps the shell realistic while account authentication, order history, and saved addresses remain intentionally unconnected."
    />
  );
}
