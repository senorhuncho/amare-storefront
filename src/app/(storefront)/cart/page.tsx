import { StorefrontPlaceholderPage } from "@/components/layout/StorefrontPlaceholderPage";

export default function CartPage() {
  return (
    <StorefrontPlaceholderPage
      eyebrow="CART"
      title="Cart creation waits for the Shopify milestone."
      description="Quick add on the homepage currently demonstrates non-blocking feedback only. Real cart persistence and checkout handoff will arrive when Shopify credentials are available."
    />
  );
}
