import { StorefrontPlaceholderPage } from "@/components/layout/StorefrontPlaceholderPage";

export default function ContactPage() {
  return (
    <StorefrontPlaceholderPage
      eyebrow="CONTACT"
      title="Support details will be connected once customer service channels are confirmed."
      description="Shipping, returns, and contact workflows stay intentionally unpublished until the store owner provides the final support inbox, policies, and response expectations."
    />
  );
}
